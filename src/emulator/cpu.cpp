#include "cpu.hpp"

CPU::CPU() = default;

CPU::~CPU() = default;


// Pulls the instruction from the program counter, and executes it.
// Returns the number of cycles used.
int CPU::execute()
{
    return 0;
}
